// app/components/Loading.tsx
import React from "react";

const Loading = () => {
  return (
    <div className="loading">
      {/* Thêm nội dung loading của bạn ở đây */}
      <p>Đang tải...</p>
    </div>
  );
};

export default Loading;
